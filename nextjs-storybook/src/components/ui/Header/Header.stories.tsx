import { ComponentMeta } from "@storybook/react";
import { Header } from "./Header";

export default {
  title: "UI/Header",
  component: Header,
} as ComponentMeta<typeof Header>;

export const Basic1 = {
  args: {},
};
